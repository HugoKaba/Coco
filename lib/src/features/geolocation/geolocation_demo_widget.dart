import 'package:flutter/material.dart';

class GeolocationDemoPage extends StatelessWidget {
  const GeolocationDemoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Geolocation demo')),
      body: const Center(child: Text('Geolocation demo (placeholder)')),
    );
  }
}
